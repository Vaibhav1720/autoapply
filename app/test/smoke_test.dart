import 'package:flutter_test/flutter_test.dart';

/// Minimal placeholder so `flutter test` succeeds in CI until widget tests exist.
void main() {
  test('CI smoke', () {
    expect(true, isTrue);
  });
}
